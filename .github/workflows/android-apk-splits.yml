name: Android APK Splits

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: read

jobs:
  build-apk-splits:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v6                          # Node 24 native (v6.0.2)

      # pnpm must be set up BEFORE setup-node so the pnpm store path
      # is available for the cache key resolution in the next step
      - name: Setup pnpm
        uses: pnpm/action-setup@v4                         # Node 24 native (v4.x)
        with:
          version: 10
          run_install: false

      - name: Set up Node 24
        uses: actions/setup-node@v5                        # Node 24 native (v5.x)
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Set up Java 21
        uses: actions/setup-java@v5                        # Node 24 native (v5.0.0)
        with:
          distribution: temurin
          java-version: 21

      # --ignore-workspace prevents "packages field missing or empty" when
      # pnpm-workspace.yaml exists with only onlyBuiltDependencies (no packages:)
      - name: Install JS dependencies
        run: pnpm install --frozen-lockfile --ignore-workspace

      # MUST run BEFORE any android/ changes — --clean deletes & regenerates the
      # entire android/ folder, wiping any earlier modifications
      - name: Expo prebuild
        run: pnpm exec expo prebuild --platform android --clean --non-interactive
        env:
          ANDROID_SDK_ROOT: /usr/local/lib/android/sdk

      # expo prebuild pins the wrapper to whatever Gradle version ships in the
      # SDK template. Force-upgrade to 9.5.1 (latest stable) afterward.
      # Must run wrapper twice — first updates properties, second replaces the JAR.
      - name: Upgrade Gradle wrapper to 9.5.1
        working-directory: android
        run: |
          chmod +x gradlew
          ./gradlew wrapper --gradle-version=9.5.1 --distribution-type=bin
          ./gradlew wrapper --gradle-version=9.5.1 --distribution-type=bin

      # Placed AFTER prebuild so --clean doesn't delete the keystore
      - name: Decode release keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode \
            > android/app/release.keystore

      # Python handles the generated Groovy file reliably across SDK template
      # versions; PowerShell regex is fragile when indentation/structure changes
      - name: Inject signing + splits into build.gradle
        run: |
          python3 - << 'PYEOF'
          import re, pathlib

          path = pathlib.Path("android/app/build.gradle")
          text = path.read_text(encoding="utf-8")

          signing_block = """
              signingConfigs {
                  release {
                      storeFile file("release.keystore")
                      storePassword System.getenv("ANDROID_KEYSTORE_PASSWORD")
                      keyAlias System.getenv("ANDROID_KEY_ALIAS")
                      keyPassword System.getenv("ANDROID_KEY_PASSWORD")
                  }
              }
          """

          splits_block = """
              splits {
                  density {
                      enable true
                      reset()
                      include "mdpi", "hdpi", "xhdpi", "xxhdpi", "xxxhdpi"
                      compatibleScreens "small", "normal", "large", "xlarge"
                  }
                  abi {
                      enable true
                      reset()
                      include "armeabi-v7a", "arm64-v8a", "x86", "x86_64"
                      universalApk true
                  }
              }
          """

          if "signingConfigs" not in text:
              text = re.sub(r'(android\s*\{)', r'\1' + signing_block, text, count=1)
              print("Injected signingConfigs")
          else:
              print("signingConfigs already present")

          if "splits" not in text:
              text = re.sub(r'(android\s*\{)', r'\1' + splits_block, text, count=1)
              print("Injected splits")
          else:
              print("splits already present")

          if "signingConfig signingConfigs.release" not in text:
              text = re.sub(
                  r'(buildTypes\s*\{[^}]*?release\s*\{)',
                  r'\1\n            signingConfig signingConfigs.release',
                  text, count=1, flags=re.DOTALL
              )
              print("Wired signingConfig into buildTypes.release")
          else:
              print("signingConfig already wired")

          path.write_text(text, encoding="utf-8")
          PYEOF

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6               # Node 24 native (v6.x)

      - name: Tune Gradle performance
        run: |
          cat >> android/gradle.properties << 'EOF'
          org.gradle.jvmargs=-Xmx4g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
          org.gradle.parallel=true
          org.gradle.caching=true
          org.gradle.configureondemand=true
          EOF

      - name: Build release APKs
        working-directory: android
        env:
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: ./gradlew :app:assembleRelease --build-cache --parallel --stacktrace

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v6                   # Node 24 native (v6.x)
        with:
          name: curium-apk-splits-${{ github.ref_name }}-${{ github.run_number }}
          path: android/app/build/outputs/apk/release/*.apk
          if-no-files-found: error
          retention-days: 14
